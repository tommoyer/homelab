---
- name: Bootstrap new nodes for Ansible management
  hosts: "{{ target_hosts | default('all') }}"
  gather_facts: yes
  remote_user: root  # Initial connection as root
  pre_tasks:
    - name: Check if bootstrap user exists
      ansible.builtin.command: "getent passwd {{ bootstrap_user_name }}"
      register: _bootstrap_user_check
      failed_when: false
      changed_when: false

    - name: Skip bootstrap if user already exists
      meta: end_play
      when: _bootstrap_user_check.rc == 0
  vars:
    bootstrap_user_name: ansible
    bootstrap_user_home: /home/ansible
    bootstrap_ssh_pubkey_path: "~/.ssh/ansible.pub"
    bootstrap_ssh_key: "{{ lookup('file', bootstrap_ssh_pubkey_path) }}"  # Control node key
    kanidm_ppa_component: stable
  
  tasks:
    - name: Restore Kanidm PPA signing key when source list is present
      ansible.builtin.get_url:
        url: https://kanidm.github.io/kanidm_ppa/kanidm_ppa.asc
        dest: /etc/apt/trusted.gpg.d/kanidm_ppa.asc
        owner: root
        group: root
        mode: '0644'

    - name: Configure Kanidm PPA apt repository
      ansible.builtin.apt_repository:
        repo: >-
          deb [arch=amd64,arm64 signed-by=/etc/apt/trusted.gpg.d/kanidm_ppa.asc]
          https://kanidm.github.io/kanidm_ppa
          {{ ansible_distribution_release }}
          {{ kanidm_ppa_component }}
        filename: kanidm_ppa
        state: present
      
    - name: Install essential packages
      ansible.builtin.apt:
        name:
          - sudo
          - python3
          - python3-apt
          - aptitude
        state: present
        update_cache: yes
      when: ansible_facts['os_family'] == "Debian"

    - name: Create ansible group
      ansible.builtin.group:
        name: "{{ bootstrap_user_name }}"
        state: present

    - name: Create ansible user
      ansible.builtin.user:
        name: "{{ bootstrap_user_name }}"
        group: "{{ bootstrap_user_name }}"
        groups: sudo
        shell: /bin/bash
        create_home: yes
        home: "{{ bootstrap_user_home }}"
        state: present

    - name: Ensure ansible remote_tmp directory exists
      ansible.builtin.file:
        path: "{{ bootstrap_user_home }}/.ansible/tmp"
        state: directory
        owner: "{{ bootstrap_user_name }}"
        group: "{{ bootstrap_user_name }}"
        mode: '0700'

    - name: Set up authorized_keys for ansible user
      ansible.posix.authorized_key:
        user: "{{ bootstrap_user_name }}"
        state: present
        key: "{{ bootstrap_ssh_key }}"
        exclusive: yes

    - name: Configure passwordless sudo for ansible user
      ansible.builtin.copy:
        content: "{{ bootstrap_user_name }} ALL=(ALL) NOPASSWD:ALL\n"
        dest: "/etc/sudoers.d/{{ bootstrap_user_name }}"
        mode: '0440'
        validate: 'visudo -cf %s'

    - name: Test ansible user sudo access
      ansible.builtin.command: sudo -l
      become: yes
      become_user: "{{ bootstrap_user_name }}"
      changed_when: false

    # Disable console autologin by removing override.conf
    - name: Remove getty/console autologin override
      file:
        path: "{{ item }}"
        state: absent
      loop:
        - /etc/systemd/system/getty@tty1.service.d/override.conf
        - /etc/systemd/system/container-getty@1.service.d/override.conf
      notify: Reload systemd

    # Enable root login and password auth in SSH
    - name: Enable PermitRootLogin in sshd_config
      lineinfile:
        path: /etc/ssh/sshd_config
        regexp: '^#?{{ item.regex }}'
        line: "{{ item.line }}"
        state: present
        backup: true
      loop:
        - { regex: 'PermitRootLogin', line: 'PermitRootLogin yes' }
        - { regex: 'PasswordAuthentication', line: 'PasswordAuthentication yes' }
      notify: Restart SSH

  handlers:
    - name: Reload systemd
      systemd:
        daemon_reload: true

    - name: Restart SSH
      service:
        name: "{{ 'ssh' if ansible_os_family == 'Debian' else 'sshd' }}"
        state: restarted